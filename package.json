{
  "name": "asg1",
  "version": "1.0.0",
  "description": "``` docker build -t asg1 . ``` ## Running Docker image ``` docker run asg1 ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mostakim Mullick",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
